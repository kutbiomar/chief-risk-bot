# MVP2 — Deferred Scope Archive

These docs were drafted as a private-markets / reconciliation extension to ChiefRiskBot but are **not the live product**. They are preserved here for reference only.

The shipped v1 covers public-markets risk briefings (cockpit, liquidity, briefings, documents, positions, scenarios). MVP2's NAV reconciliation, alert rules, and email ingestion are out of scope until v1 is monetized.

- `MVP2_SPEC.md` — original spec
- `MVP2_STATUS.md` — pre-build status snapshot (stale; supersede with `admin/status/STATUS.md`)

Do not treat anything in this folder as authoritative for the live system.
