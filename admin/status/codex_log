# Codex Execution Log

## Archive

Entries 0–38 (2026-04-07 to 2026-04-08) covered Phases A–D architecture review,
schema validation, VaR design, agent orchestration, auth hardening, frontend audit,
and demo polish. All completed. See `CODEBASE_STATUS.md` Completed table for outcomes.

---

## Active — Phase E (MVP2)

Tasks E1–E9 defined in `admin/status/MVP2_STATUS.md`.
Log new Codex runs below as they execute.

---

## 2026-04-09

### Queue Item Bootstrap

- Source queue entry: `/codex-bitch queue-bootstrap`
- Scope executed: reviewed the active Phase E status docs, converted the pointer-only Phase E section in `admin/status/CODEBASE_STATUS.md` into an executable E1-E9 queue, and preserved the existing completed-history table.

### Work Performed

- Reviewed `admin/status/MVP2_STATUS.md`, `admin/status/CODEBASE_STATUS.md`, and `admin/status/codex_log`.
- Confirmed that `admin/status/MVP2_STATUS.md` is the only in-repo Phase E document that currently contains a complete E1-E9 task table.
- Added queue rows with dependency order, source commands, primary targets, and exit criteria so future `/codex-bitch` runs can execute against a concrete backlog.

### Notes

- There is a source-of-truth mismatch in the workspace: `/Users/omarkutbi/Documents/Claude/Projects/Chief risk bot/MVP2_STATUS.md` describes a different MVP2 track than `admin/status/MVP2_STATUS.md`.
- `CODEBASE_STATUS.md` now reflects the admin/status Phase E plan; if the root `MVP2_STATUS.md` is intended to replace it, the queue should be regenerated from that document before implementation starts.

### Queue Item E1-E9 Closeout

- Source queue entry: `/codex-bitch phase-e-closeout`
- Scope executed: reconciled the Phase E queue docs against the implemented MVP2 codebase and cleared the stale pending backlog.

### Work Performed

- Reviewed `admin/status/MVP2_STATUS.md`, `admin/status/CODEBASE_STATUS.md`, and the implemented Phase E backend/frontend work.
- Updated `admin/status/MVP2_STATUS.md` so E1-E9 are all marked complete with the actual delivered architecture, including the FastAPI + APScheduler + `async_jobs` execution model and the document review APIs.
- Cleared the `Pending` queue section in `admin/status/CODEBASE_STATUS.md` and added E1-E9 completion rows to the Completed table.
- Preserved the queue history while making the current state explicit: Phase E is complete and no pending `/codex-bitch` items remain.

### Notes

- The status docs now match the implemented MVP2 code more closely than the original queue text, which still referenced a Celery worker that was intentionally not introduced.
- Remaining work after queue closeout is operational rather than queue-backed: apply migrations in runtime environments, browser-smoke the updated documents review UI, and expand real-document extraction fixtures if desired.

---

## Phase F — Frontend (MVP2 UI)

Tasks defined below. Execute with `/codex-bitch` in dependency order.

| # | Task | Scope | Priority | Status |
|---|------|-------|----------|--------|
| F1 | **Document extraction review UI** | Wire `GET /api/documents/{id}/review` into `documents.html`. Render extraction artifacts per field: extracted value, confidence score (colour-coded), agent source label. Wire `PATCH /api/documents/{id}/review` to the "Save review" button. Wire instructions fields must show HITL badge and be always-editable. | P0 | Completed 2026-04-09 |
| F2 | **Factor tag editor in positions table** | Add 7 `factor_*` columns to `table.html` position rows (asset_class, sector, subsector, market_segment, country, region, tag_source). Inline-editable. Show `factor_tag_confidence` as a subtle badge. PATCH to `PUT /api/portfolio/positions/{id}`. | P0 | Completed 2026-04-09 |
| F3 | **Overlay standalone page** (`overlay.html`) | New page linked from cockpit nav. Full factor scoring table (all 8 proxy baskets, z-scores, today's score). Regime indicator with trigger conditions. Stress scenario table (all 6 scenarios, estimated $ impact, severity badge). "Run overlay" button → `POST /api/overlay/run`. Auto-refresh every 60 s. | P1 | Completed 2026-04-09 |
| F4 | **Document parse pipeline progress UI** | After clicking "Parse", poll `GET /api/documents/{id}` (or a job status endpoint) and show per-agent status badges: librarian → accountant → risk_officer → treasury → reconciliation. Show final confidence summary on completion. Replace the static spinner. | P1 | Completed 2026-04-09 |
| F5 | **Cockpit overlay panel polish** | The existing cockpit overlay section shows max 6 factors and 4 stress scenarios. Expand to show all items with scroll, add regime change timestamp, add "View full overlay →" link to `overlay.html` (F3), and surface any active alerts as dismissible banners above the fold. | P2 | Completed 2026-04-09 |

### Build order

```
F1 (doc review)  → F4 (parse progress)
F2 (factor tags) → F3 (overlay page) → F5 (cockpit polish)
```

F1 and F2 are independent and can run in parallel.

### Queue Item F1

- Source queue entry: `/codex-bitch F1`
- Scope executed: completed the MVP2 document extraction review UI in `frontend-mvp/documents.html` and `frontend-mvp/_app.js`, and extended the review API so editable wire instruction fields persist correctly.

### Work Performed

- Added `capital_calls` and `distribution_notices` upload folders in `frontend-mvp/documents.html` so the review flow can be exercised against capital-call and distribution documents from the UI.
- Reworked the documents preview in `frontend-mvp/_app.js` into a review workspace that renders artifact fields with extracted values, confidence pills, and agent source labels for librarian, treasury, risk, and layout outputs.
- Added always-editable wire instruction fields with explicit HITL badges and kept the save action wired to `PATCH /api/documents/{id}/review`.
- Fixed a review-state bug by basing save payloads on the current review object instead of rebuilding partial position rows from visible inputs only, which preserves `source_ref` and extracted factor tags during manual edits.
- Extended `backend/schemas/content.py`, `backend/routers/documents.py`, and `backend/services/documents.py` so review saves can persist treasury edits (`wire_bank`, `wire_account`, `wire_routing`, `wire_reference`, contacts) instead of dropping them.
- Added coverage in `backend/tests/test_phase_cd.py` for persisted treasury edits during the capital-call review flow.

### Notes

- Verification passed with `.venv/bin/python -m pytest backend/tests/test_phase_cd.py -k capital_call_with_wire_instructions_requires_review` and `node --check frontend-mvp/_app.js`.
- Remaining Phase F queue items are still open: F2 factor tag editor, F3 overlay page, F4 parse progress UI, F5 cockpit overlay polish.

### Queue Items F2-F5

- Source queue entry: `/codex-bitch F2-F5`
- Scope executed: cleared the remaining MVP2 frontend queue by extending the factor-tag contract, finishing the positions table editor, adding a standalone overlay page, replacing the document parse spinner with pipeline progress, and polishing the cockpit overlay surface.

### Work Performed

- Extended the portfolio position model, schemas, router, extraction approval flow, and migration chain to support `factor_country`, `factor_tag_source`, and `factor_tag_confidence`, then added backend coverage for persisted factor-tag updates.
- Updated `frontend-mvp/table.html`, `frontend-mvp/_app.js`, and `frontend-mvp/_mvp.css` so positions expose inline-editable factor tag fields directly in the table plus matching fields in the side editor, with confidence rendered as a compact badge.
- Added `frontend-mvp/overlay.html`, linked it from `frontend-mvp/_shell.js`, and wired `frontend-mvp/_app.js` to render full overlay factor scoring, regime diagnostics, AUM triangulation, all stress scenarios, live refresh, and an auto-refresh timer.
- Extended `/api/overlay/regime` to expose `created_at` so the UI can show when the regime state was last updated.
- Replaced the documents parse spinner with staged agent badges (`librarian` → `accountant` → `risk_officer` → `treasury` → `reconciliation`), lightweight polling of the selected document, and a final parse confidence summary after completion.
- Polished the cockpit overlay section to show all triangulation and stress rows with scroll, render dismissible overlay alert banners above the fold, show the regime update timestamp, and link through to the full overlay page.

### Notes

- Verification passed with `.venv/bin/python -m pytest backend/tests/test_overlay.py backend/tests/test_phase_cd.py -k 'overlay or factor_tag or capital_call_with_wire_instructions_requires_review'` and `node --check frontend-mvp/_app.js`.
- The Phase F queue is now clear.

---

## Phase G — Pre-Land Review Findings (2026-04-09)

### Source

`/GSreview` — full pre-landing review of MVP2 branch vs `main`.
138 files changed, 16,283 insertions, 1,926 deletions.
Specialists dispatched: testing, security, performance, data-migration, maintainability + adversarial subagent (Claude).
15/15 existing tests pass post-auto-fix.

### Auto-Fixed (8 items — already applied to branch)

| File | Fix |
|------|-----|
| `backend/deps.py:12` | Deleted dead `get_current_user()` stub |
| `backend/services/var.py:33` | **Bug:** `worst_scenario_date` always returned today — fixed to `utc_now() - timedelta(days=worst_index)` |
| `backend/services/var.py` | Added `LOOKBACK_DAYS = 252` constant |
| `backend/services/enrichment.py` | Added `CACHE_TTL_HOURS = 4` constant; replaced 5 literals |
| `backend/schemas/auth.py:17` | Added `min_length=8` validator to `ResetPasswordRequest.new_password` |
| `backend/services/ingest/csv_parser.py:65` | Formula cells (`=`,`+`,`-`,`@`) now raise `ValueError` instead of silently prepending `'` |
| `backend/routers/settings.py:70` | Added explicit `_PATCHABLE` allowlist to `setattr` loop |
| `frontend-mvp/_app.js:383` | Removed TOTP stub hint ("Use code 000000") from login UI |
| `.DS_Store` | Untracked from git (`git rm --cached`) |

### Pending — Must Fix Before Landing (P0)

| # | File | Issue |
|---|------|-------|
| G1 | `backend/routers/auth.py:216` | **TOTP bypass** — `payload.code != "000000"` is the real check. Ships an MFA backdoor. Fix: `pyotp.TOTP(user.totp_secret).verify(payload.code)` or disable `totp_enabled` entirely until implemented. |
| G2 | `backend/services/ingest/pipeline.py` | **asyncio crash** — `asyncio.run()` called inside a FastAPI async route. Every document parse crashes under Uvicorn. Fix: make `parse_document` async or run via `BackgroundTasks`. |
| G3 | `backend/routers/auth.py` + all state-mutating routes | **CSRF only on logout** — `require_cookie_csrf` is missing on POST /portfolio/positions, POST /ingest/csv, POST /risk/run, POST /documents/upload, PATCH /settings, and ~8 other routes. Fix: add `Depends(require_cookie_csrf)` to all state-mutating routes. |
| G4 | `backend/routers/portfolio.py` | **Snapshot is_current race** — no `SELECT FOR UPDATE`; two concurrent requests can produce two snapshots with `is_current=True`. Fix: atomic `UPDATE … WHERE is_current=True AND rowcount==1` + DB-level partial unique index. |
| G5 | `backend/routers/auth.py:106` | **API key resolves to oldest user** — not the key owner. Bypasses any future RBAC. Fix: add `user_id` FK to `ApiKey`, resolve to that user. |
| G6 | Tests — `backend/tests/` | **Zero workspace isolation tests** — no test verifies user A cannot access user B's data. An IDOR regression is invisible. Fix: add cross-workspace fixture covering all routers. |
| G7 | Tests — `backend/tests/` | **No unauthenticated-access tests** — no test hits a protected route without credentials and asserts 401. |

### Pending — Should Fix Before Landing (P1)

| # | File | Issue |
|---|------|-------|
| G8 | `backend/services/auth/password.py:6` | PBKDF2 → Argon2id. PBKDF2 is GPU-parallelisable. Switch to `CryptContext(schemes=["argon2"])`. |
| G9 | `backend/services/enrichment.py:251` | N+1 queries — one `db.get(PriceCache, ticker)` per position in a loop. Fix: batch fetch with `WHERE ticker IN (...)`. Same in `backend/routers/market.py:41`. |
| G10 | `backend/services/enrichment.py` | `PriceCache` / `MacroCache` TTL never checked on read — stale data from days ago used for VaR. Fix: add freshness check before returning cached row. |
| G11 | `backend/services/risk.py:517` | `asyncio.get_event_loop()` deprecated in Python 3.10+. Fix: refactor `run_risk_analysis` to `async def` or use `asyncio.get_running_loop()`. |
| G12 | `backend/services/risk.py:224` | `ai_model` hardcoded as `claude-sonnet-4-6` — workspace setting is never read. |
| G13 | `backend/routers/briefings.py:67` + `documents.py:73` | List endpoints have no `LIMIT` — unbounded result sets. Add pagination. |

### Pending — Good to Fix (P2)

| # | Issue |
|---|-------|
| G14 | Missing composite DB indexes: `portfolio_snapshots(workspace_id, is_current)`, `asset_factor_exposures(snapshot_id, factor_key)`, `risk_regimes(workspace_id, as_of_date)`. |
| G15 | `ASSET_CLASS_ALIASES` duplicated in `documents.py` and `csv_parser.py` — move to `backend/constants.py`. |
| G16 | Audit log hash chain broken under concurrent Postgres writes — sequence numbers assigned in Python, not DB. Add `UNIQUE` constraint. |
| G17 | `briefing_auto_publish` publishes with no quality threshold — can auto-publish an all-deterministic-fallback briefing. |
| G18 | Relative storage paths (`backend/runtime/storage/...`) — break if app is started from a non-root dir. Fix: anchor to `Path(__file__).parent`. |

### PR Quality Score

**3.5 / 10** — architecture is solid but G1 (TOTP bypass), G2 (asyncio crash), and G3 (CSRF gap) must be resolved before landing. Fix those three and the score is ~7/10.

### Phase G Review Refresh (2026-04-09)

- Source: refreshed pre-landing review for the MVP2 branch after 8 auto-fixes were already applied.
- This entry supersedes the blocker counts in the initial Phase G write-up above and preserves the source wording from the latest review.
- Tests still pass: 15/15.

#### Critical (9)

| # | File | Issue |
|---|------|-------|
| 1 | `backend/routers/auth.py:216` | TOTP bypass ships to production. `payload.code != "000000"` is the actual check. Recommended fix: `pyotp.TOTP(user.totp_secret).verify(payload.code)` or disable `totp_enabled` until real verification exists. |
| 2 | `backend/config.py:19` | Default `secret_key = "replace-me"` passes validation. Recommended fix: reject the default outside development with a field validator. |
| 3 | State-mutating cookie-auth routes | CSRF enforced only on logout/logout-all. Recommended fix: add `Depends(require_cookie_csrf)` to all POST/PATCH/DELETE routes that accept session-cookie auth or enforce it globally. |
| 4 | `backend/routers/portfolio.py` | Snapshot `is_current` race condition. Recommended fix: atomic `UPDATE ... WHERE is_current=True` with `rowcount == 1`, plus a DB-level uniqueness guard. |
| 5 | `backend/services/ingest/pipeline.py` | `asyncio.run()` is called from a FastAPI async flow, which crashes under Uvicorn. Recommended fix: make the pipeline async and `await` it, or run it via background jobs. |
| 6 | `backend/services/enrichment.py:251`, `backend/routers/market.py:41` | N+1 queries over `PriceCache` on hot paths. Recommended fix: batch fetch `WHERE ticker IN (...)` and index into a dict in memory. |
| 7 | `backend/routers/auth.py:106-115` | `_resolve_api_key_user` resolves to the oldest workspace user, not the API key owner. Recommended fix: add `user_id` to `ApiKey` and resolve directly. |
| 8 | `backend/services/var.py` | `worst_scenario_date` bug was critical in the review but is already auto-fixed on this branch. |
| 9 | `backend/tests/` | No workspace isolation tests exist. Recommended fix: add cross-workspace access tests for documents, positions, briefings, and overlay data. |

#### Informational (9)

| # | File | Issue |
|---|------|-------|
| 10 | `backend/services/auth/password.py:6` | PBKDF2 should move to Argon2id for stronger password hashing. |
| 11 | `backend/services/enrichment.py` | `PriceCache` and `MacroCache` TTL is defined but never enforced on read, so stale data can flow into VaR. |
| 12 | `backend/services/risk.py:517` | `asyncio.get_event_loop()` is deprecated in modern Python. |
| 13 | `backend/services/risk.py:224` | `ai_model` is hardcoded; workspace settings are ignored. |
| 14 | `backend/routers/briefings.py:67`, `backend/routers/documents.py:73` | Briefings/documents list endpoints are unpaginated. |
| 15 | DB indexes | Missing composite indexes on hot query paths such as `portfolio_snapshots(workspace_id, is_current)` and `asset_factor_exposures(snapshot_id, factor_key)`. |
| 16 | `backend/services/audit/logger.py` | Audit hash-chain sequencing can race on Postgres because sequence numbers are assigned in Python. |
| 17 | `backend/tests/` | No unauthenticated-access regression tests assert 401 on protected routes. |
| 18 | Shared constants | `ASSET_CLASS_ALIASES` is duplicated in `documents.py` and `csv_parser.py`; move it to a shared module. |

#### Scope / Score

- Scope check: clean. The diff still matches MVP2 Phase E + F intent.
- PR quality score remains **3.5 / 10**.
- Highest-priority fixes before landing: TOTP bypass, CSRF coverage, and the `asyncio.run()` crash.

---

## Phase H — Demo QA Findings (2026-04-10)

### Source

`/qa` — browser-based QA run against `http://localhost:8000` (frontend) + `http://localhost:8001` (backend).
Demo credentials: `cio@demo.chiefriskbot.com` / `DemoPass2026!`

### Auto-Fixed (1 item — already committed)

| # | File | Fix |
|---|------|-----|
| H1 | `frontend-mvp/*.html` (all 8 pages) | **`_shell.css` 404 on every page** — all pages referenced `../frontend-design-ideal/_shell.css` but `serve.js` exposes `frontend-design-ideal/` as a root, so the correct path is `/_shell.css`. This silently killed the entire design system (warm cream palette, Fraunces headlines, Inter Tight, JetBrains Mono) on every page load. Fixed with a `sed` replace across all 8 files. Committed in `c3bd7d3`. |

### Pending — Must Fix Before Demo (P0)

| # | Area | Issue | Fix |
|---|------|-------|-----|
| H2 | **Server restart needed** | The running `uvicorn` process (pid 26193, started before overlay routes were added) does not serve `GET /api/overlay/*` — returns 404. Routes exist in code (`backend/routers/overlay.py`) and register correctly when the app is freshly imported. Backend must be restarted before the demo. | Restart uvicorn: `pkill -f 'uvicorn backend.main' && cd /path/to/repo && .venv/bin/uvicorn backend.main:app --port 8001 --log-level warning` |
| H3 | **Cockpit empty** | Cockpit shows blank "AUM triangulation", "Top contributors", and "Active risks" sections because the overlay endpoints return 404 (see H2). Once backend is restarted the cockpit should populate from `GET /api/cockpit` + `GET /api/overlay/*`. Verify after H2 fix. | Fix H2 first, then re-run seed (`admin/demo/seed_demo.py`) to ensure overlay data exists. |
| H4 | **Overlay page all-empty** | `overlay.html` shows "factors unavailable", "triangulation unavailable", "regime unavailable", "scenarios unavailable" for the same reason as H3. | Fix H2. If still empty after restart, run "Run overlay" button to trigger `POST /api/overlay/run`. |
| H5 | **Briefings page white-blank on fresh browse session** | After a new headless browser session, navigating directly to `briefings.html` redirects to login correctly — but the subsequent screenshot captured the redirect in-flight (blank). Not a real user-facing bug. Verified working during logged-in session. Low risk. | No code change needed. Confirm manually after demo seed. |

### Pending — Should Fix Before Demo (P1)

| # | Area | Issue | Fix |
|---|------|-------|-----|
| H6 | **Demo seed must be re-run** | The existing `cio@demo.chiefriskbot.com` DB record was created by an older seed run and has no overlay data (factor scores, regime, stress scenarios). Run `admin/demo/seed_demo.py` to wipe and reseed a clean state with all data. | `.venv/bin/python admin/demo/seed_demo.py` |
| H7 | **Cockpit AUM shows blank** | `GET /api/cockpit` returns `aum_usd: null` and `position_count: null` — the cockpit endpoint may not be reading from the current snapshot correctly, or the snapshot is not being returned for the demo workspace. Debug after H6. | Check `backend/routers/cockpit.py` — verify `is_current=True` query matches the workspace. |

### Demo Readiness Checklist

Before showing the demo:

```
[ ] Restart uvicorn (H2)
[ ] Run admin/demo/seed_demo.py (H6)
[ ] Open http://localhost:8000/login.html
[ ] Log in: cio@demo.chiefriskbot.com / DemoPass2026!
[ ] Verify cockpit shows AUM + risk chart + overlay panel
[ ] Click "Overlay" nav → verify all 4 sections populated
[ ] Click "Briefings" → verify published briefing appears
[ ] Click briefing → verify full HTML renders
[ ] Click "Positions" → verify table with factor tags
[ ] Click "Documents" → verify custodian statement + extraction panel
```
